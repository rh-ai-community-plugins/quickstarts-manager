{
  "name": "quickstarts-manager-bff",
  "version": "0.1.0",
  "description": "BFF service for the Quickstarts Manager RHOAI dashboard plugin",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "start:dev": "npx tsx watch src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src/",
    "validate": "npm run typecheck && npm run lint && npm test"
  },
  "dependencies": {
    "express": "^4.21.0",
    "https-proxy-agent": "^7.0.6",
    "js-yaml": "^5.2.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^30.0.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "<rootDir>/jest.setup.ts"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.ts"
    ],
    "moduleNameMapper": {
      "^https-proxy-agent$": "<rootDir>/__mocks__/https-proxy-agent.ts"
    },
    "collectCoverageFrom": [
      "src/**/*.ts",
      "!src/**/*.d.ts"
    ]
  }
}
